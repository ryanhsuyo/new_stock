# H7.12 Update Workflow Box Boundary

Phase: Dashboard Gradual Split

Status: done

Read first:

* `backend/docs/ai_execution_plan.md`
* `backend/docs/ai_tasks/loop_state.md`
* `frontend/src/pages/Dashboard.tsx`

Scope:

* Extract only the update workflow presentation box.
* Do not change workflow service contracts or backend behavior.
* Keep the new component file under 200 lines.

## TASK H712-01 — Extract Update Workflow Box

Status: done

Goal:
Move `UpdateWorkflowBox` out of `Dashboard.tsx`.

Steps:

* Create `frontend/src/components/UpdateWorkflowBox.tsx`.
* Move self-contained copy-command behavior.
* Preserve rendered text and step display.

Acceptance:

* Dashboard renders the moved component.
* No workflow status or priority logic changes.

Result:

* Added `frontend/src/components/UpdateWorkflowBox.tsx`.
* Dashboard imports the moved update workflow box.

## TASK H712-02 — Verification

Status: done

Goal:
Verify the frontend still builds.

Steps:

* Run `npm run build` in `frontend`.
* Inspect `git diff --stat`.

Acceptance:

* Build passes.
* Only expected component/dashboard/task files changed.

Result:

* `npm run build` passed in `frontend`.
* Existing Vite chunk-size warning remains informational.

## Phase Checklist

* [x] H712-01 Extract update workflow box
* [x] H712-02 Verification
