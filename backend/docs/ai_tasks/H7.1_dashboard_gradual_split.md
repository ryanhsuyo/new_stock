# H7.1 Dashboard Gradual Split Tasks

Phase: Dashboard Gradual Split

Status: done

Read first:

* `backend/docs/ai_execution_plan.md`
* `backend/docs/ai_tasks/loop_state.md`
* `backend/docs/status_overview.md`

Scope:

* Reduce `Dashboard.tsx` size with one small component boundary.
* Do not move strategy logic to frontend.
* Do not change API contracts.
* Do not perform large refactoring.

## TASK H71-01 — Split Candidate Inventory

Status: done

Goal:
Find one self-contained Dashboard component that can move safely.

Steps:

* Inspect top-level Dashboard helper components.
* Prefer pure or mostly-presentational code.
* Avoid components with many callbacks or business rules.

Acceptance:

* Inventory notes identify one safe target or explain why none is safe.

## TASK H71-02 — Extract One Component

Status: done

Goal:
Move one small component to `frontend/src/components/`.

Steps:

* Preserve props and behavior.
* Keep imports simple.
* Do not add strategy logic.

Acceptance:

* Dashboard imports the extracted component.
* No user-facing behavior changes intended.

## TASK H71-03 — Verification

Status: done

Goal:
Verify the extraction.

Steps:

* Run `npm run build` from `frontend`.
* Check AI-facing markdown line counts.

Acceptance:

* Build passes.
* AI-facing docs remain under 200 lines.

Verification result:

* `npm run build` passed.
* AI-facing docs checked under 200 lines.

## Inventory Notes

Status: filled

Chosen target:

* `ParseErrorAlert` from `Dashboard.tsx`.

Why safe:

* Pure presentational component.
* Only reads `SignalsStatus`.
* No callbacks, API calls, or strategy logic.
* Existing Dashboard call site can keep the same prop.

## Phase Checklist

* [x] H71-01 Split candidate inventory
* [x] H71-02 Extract one component
* [x] H71-03 Verification
