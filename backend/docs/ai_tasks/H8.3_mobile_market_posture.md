# H8.3 Mobile Market Posture Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Keep Market Posture unchanged on desktop while showing it as a collapsed disclosure after Today Focus on phones.

**Architecture:** `MarketPostureCard` owns both visual variants and shares one content renderer. Dashboard renders the desktop card inside its grid and the mobile disclosure immediately after the grid with identical backend-derived props. CSS switches variants only at `max-width: 560px`.

**Tech Stack:** React 18, TypeScript, CSS, Node built-in test runner

---

Status: done

Hard rules:

* Do not add frontend strategy logic.
* Do not change backend contracts or trading data.
* Do not add dependencies.
* Desktop layout and behavior must remain unchanged.
* Do not commit unless explicitly requested.

### Task 1: Add the responsive structure contract

**Files:**

* Create: `frontend/tests/mobile-market-posture.test.mjs`

- [x] Add a Node test that asserts:
  - `MarketPostureCard` supports a disclosure variant.
  - Dashboard renders both desktop and mobile variants.
  - CSS hides the mobile variant by default and switches variants at 560px.
- [x] Run `node --test tests/mobile-market-posture.test.mjs`.
- [x] Confirm it fails because the disclosure variant is missing.

### Task 2: Add the disclosure variant

**Files:**

* Modify: `frontend/src/components/MarketPostureCard.tsx`
* Modify: `frontend/src/pages/Dashboard.tsx`
* Modify: `frontend/src/App.css`

- [x] Add `variant?: 'card' | 'disclosure'` to `MarketPostureCardProps`.
- [x] Reuse one body fragment for reason, metadata, sectors, and stale-note action.
- [x] Render the existing card for `variant="card"`.
- [x] Render a collapsed native `details` with title summary for `variant="disclosure"`.
- [x] Render the disclosure after `.decision-console-grid` using identical props.
- [x] Hide `.market-posture-mobile` by default.
- [x] At `max-width: 560px`, hide `.market-posture-desktop` and show the mobile disclosure.

### Task 3: Verify and reconcile status

**Files:**

* Modify: `backend/docs/homepage_pm_roadmap.md`
* Modify: `backend/docs/ai_tasks/H8.3_mobile_market_posture.md`
* Modify: `backend/docs/ai_tasks/loop_state.md`
* Modify: `backend/docs/ai_execution_plan.md`

- [x] Run `node --test tests/mobile-market-posture.test.mjs` and confirm it passes.
- [x] Run `npm run build` and confirm TypeScript/Vite pass.
- [x] Run `git diff --check`.
- [x] Mark HP-015 and H8.3 complete, preserving the H5.0 screenshot limitation.

## Phase Checklist

* [x] Responsive structure contract
* [x] Disclosure implementation
* [x] Verification and docs reconciliation

## Verification Evidence

* RED: all three structure tests failed before the disclosure implementation.
* GREEN: `node --test tests/mobile-market-posture.test.mjs` passed 3 tests.
* `npm run build` passed; the existing Vite chunk-size warning remains.
