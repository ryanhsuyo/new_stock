# H2.6 Homepage Readability Tasks

Phase: Homepage Readability & Mobile First Screen

Status: done

Read first:

* `backend/docs/ai_execution_plan.md`
* `AGENTS.md`
* `claude.md`
* `backend/docs/current_rules.md`
* `backend/docs/signal_rules.md`
* `backend/docs/homepage_pm_roadmap.md`

Scope:

* Reduce first-screen reading load.
* Keep backend strategy contracts as source of truth.
* Make mobile first screen show only essential decision state.
* Do not add new strategy logic to frontend.
* Do not modify trades, holdings, cash, or generated outputs.

## TASK H26-01 — Baseline Inventory

Status: done

Goal:
Confirm current Dashboard and report UI hotspots before changing them.

Steps:

* Inspect `frontend/src/pages/Dashboard.tsx`.
* Inspect `frontend/src/pages/UniverseReportPage.tsx`.
* Inspect related CSS in `frontend/src/App.css`.
* Check current docs for HP-009, HP-011, HP-015.

Acceptance:

* Notes below identify the exact components/classes to touch.
* No functional code changed in this task.

## TASK H26-02 — Candidate Table Density

Status: done

Goal:
Make the candidate report easier to scan without changing strategy rules.

Steps:

* Keep table data and sorting logic unchanged.
* Ensure long reason/risk text is visually constrained by default.
* Preserve existing expand/collapse affordance if present.
* Make primary daily action, price plan, and detail button easier to spot.

Acceptance:

* Candidate rows remain navigable to detail.
* Long text does not dominate row height by default.
* No frontend strategy calculation is added.

## TASK H26-03 — Market Posture Summary

Status: done

Goal:
Make artificial/manual market note information read as a short posture summary.

Steps:

* Keep full manual note detail lower on the page.
* In Decision Console, show date, stale state, water level/risk, and focus sectors compactly.
* If stale, prefer update action over showing old long text.

Acceptance:

* First screen does not show long manual-note prose.
* Stale notes are clearly marked as needing update.
* Full detail remains available below.

## TASK H26-04 — Mobile First Screen

Status: done

Goal:
Improve mobile Dashboard first screen readability.

Steps:

* Use CSS-only or minimal markup changes.
* On narrow screens, prioritize Status Strip, Primary Action, and Today Focus.
* Keep PM Worklist and file health below the first screen.

Acceptance:

* Mobile layout avoids dense side-by-side cards.
* Primary Action remains visible before lower detail sections.
* No new API calls are added.

## TASK H26-05 — Verification

Status: done

Goal:
Verify the phase.

Steps:

* Run `npm run build` from `frontend`.
* Run focused backend tests only if backend files changed.
* Run full backend tests if any service/model contract changed.
* Check `git diff --stat`.

Acceptance:

* Frontend build passes.
* Backend tests are not required if only frontend/docs changed.
* AI-facing markdown files remain under 200 lines.

## Inventory Notes

Status: done

* Dashboard touchpoints: `DecisionConsole` and `.decision-console*` classes.
* Candidate table touchpoints: `UniverseReportPage` reason/action cells and `.daily-action-cell`, `.report-reason-cell`, `.report-no-buy-reason`, `.report-risk-note`.
* Mobile touchpoints: existing `@media (max-width: 900px)` / `560px` rules in `frontend/src/App.css`.
* Roadmap targets: HP-009, HP-011, HP-015.

## Phase Checklist

* [x] H26-01 Baseline inventory
* [x] H26-02 Candidate table density
* [x] H26-03 Market posture summary
* [x] H26-04 Mobile first screen
* [x] H26-05 Verification
