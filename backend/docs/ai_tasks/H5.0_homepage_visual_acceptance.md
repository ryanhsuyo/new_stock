# H5.0 Homepage Visual Acceptance

Phase: Homepage Visual Acceptance

Status: done

Goal:

Complete the remaining desktop and mobile browser acceptance for the Dashboard.

Required reading:

* `backend/docs/homepage_visual_acceptance.md`
* `backend/docs/homepage_pm_roadmap.md`
* `backend/docs/ai_tasks/H8.2_secondary_dashboard_disclosure.md`

Hard rules:

* Do not modify trading data or generated outputs.
* Do not change product behavior unless visual evidence identifies a defect.
* Keep fixes limited to the smallest relevant frontend layout change.
* Keep this file under 200 lines.

## TASK H50-00 — Start Local Acceptance Environment

Status: done

Steps:

* Start FastAPI on port 9000 without reload.
* Start Vite on port 5173.
* Confirm both endpoints respond before opening the browser.

Acceptance:

* Backend health or API endpoint responds.
* Frontend document responds.

## TASK H50-01 — Desktop Acceptance

Status: done_with_waiver

Steps:

* Check the Dashboard at 1280x720.
* Verify Decision Console priority and the four collapsed secondary groups.
* Verify focus navigation opens the containing group.
* Check document overflow and capture a screenshot.

Acceptance:

* No horizontal document overflow.
* Required first-screen content remains readable.
* Secondary groups are collapsed by default and independently toggleable.

## TASK H50-02 — Mobile Acceptance

Status: done_with_waiver

Steps:

* Check the Dashboard at 390x844.
* Verify Status, Primary Action, Today Focus, and Market Posture order.
* Check document overflow and capture a screenshot.

Acceptance:

* No horizontal document overflow.
* Cards stack without overlap.
* Primary decision content stays before secondary details.

## TASK H50-03 — Reconcile Evidence

Status: done

Steps:

* Record URL, viewport, data state, and screenshot result.
* Update HP-013 and loop state accurately.
* Run `npm run build` if any frontend file changes.

Acceptance:

* Browser evidence and any remaining limitation are explicit.
* Roadmap and loop state match observed behavior.

## Phase Checklist

* [x] H50-00 Start local acceptance environment
* [x] H50-01 Desktop acceptance — closed with environment waiver
* [x] H50-02 Mobile acceptance — closed with environment waiver
* [x] H50-03 Reconcile evidence

## Evidence

* URL: `http://127.0.0.1:5173` and `http://localhost:5173`.
* Local FastAPI and Vite processes started successfully without reload.
* HTTP checks returned frontend 200 and a backend HTTP response.
* The in-app browser listed its blank tab successfully but timed out navigating
  to localhost on three attempts, including after restarting services outside
  the sandbox.
* Final retry on 2026-06-19 confirmed FastAPI `/docs` and Vite `/` both returned
  HTTP 200. The in-app browser still timed out after 20 seconds while navigating
  to `http://127.0.0.1:5173`.
* A visible-browser retry produced the same timeout: the browser could display
  and list its `about:blank` tab, but `goto` did not return within 20 seconds.
  This rules out background-tab visibility as the cause.
* No screenshot or viewport claim is recorded because browser navigation did
  not complete.
* On 2026-06-19, the user explicitly approved closing desktop/mobile screenshot
  acceptance with an environment waiver. HTTP, DOM/overflow checks, structure
  tests, and frontend builds remain the recorded evidence; no screenshot is
  claimed to exist.
