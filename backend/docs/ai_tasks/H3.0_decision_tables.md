# H3.0 Decision Tables Tasks

Phase: Candidate and Holding Decision Table Improvements

Status: done

Read first:

* `backend/docs/ai_execution_plan.md`
* `backend/docs/ai_tasks/loop_state.md`
* `backend/docs/homepage_pm_roadmap.md`
* `backend/docs/current_rules.md`
* `backend/docs/signal_rules.md`

Scope:

* Improve scanability of holding and candidate decision tables.
* Keep backend contracts as the source of strategy meaning.
* Do not modify trading records, holdings, cash, or generated outputs.
* Do not introduce dependencies or large refactors.

## TASK H30-01 — Decision Table Inventory

Status: done

Goal:
Identify where candidate and holding table fields are rendered and which backend fields already provide action, price range, risk, and reason.

Steps:

* Inspect `frontend/src/pages/UniverseReportPage.tsx`.
* Inspect relevant types in `frontend/src/types/`.
* Inspect backend report/service fields only if needed.
* Note current columns, crowded fields, and safest next UI target.

Acceptance:

* Inventory notes list one safe improvement target.
* No functional code changes in this task.

## TASK H30-02 — One Safe Scanability Improvement

Status: done

Goal:
Apply one small table readability improvement without changing strategy logic.

Steps:

* Prefer rendering existing backend fields more clearly.
* Keep long reasons clamped or moved to tooltip/title.
* Preserve detail links.
* Do not create new buy/sell rules in frontend.

Acceptance:

* Table is easier to scan.
* No new frontend strategy judgment is added.

## TASK H30-03 — Verification

Status: done

Goal:
Verify the H3.0 change.

Steps:

* Run `npm run build` from `frontend`.
* Check AI-facing markdown line counts.
* Check `git diff --stat`.

Acceptance:

* Build passes.
* AI-facing docs remain under 200 lines.

Verification result:

* `npm run build` passed.
* AI-facing docs checked under 200 lines.

## Inventory Notes

Status: filled

Findings:

* `UniverseReportPage.tsx` already renders daily decision cards, priority rows, price-position filters, and a full report table.
* `UniverseReportItem` already includes close, entry range, stop, target, R/R, no-buy reason, risk note, daily action, and holding fields.
* The full table has many columns and horizontal scrolling; when scanning price/risk columns, stock identity can leave view.

Chosen safe target:

* Make the first report-table column sticky during horizontal scroll.
* This improves scanability without adding strategy logic or changing backend contracts.

## Phase Checklist

* [x] H30-01 Decision table inventory
* [x] H30-02 One safe scanability improvement
* [x] H30-03 Verification
