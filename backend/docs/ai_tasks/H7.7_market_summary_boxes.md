# H7.7 Market Summary Boxes Boundary

Phase: Dashboard Gradual Split

Status: done

Read first:

* `backend/docs/ai_execution_plan.md`
* `backend/docs/ai_tasks/loop_state.md`
* `frontend/src/pages/Dashboard.tsx`

Scope:

* Extract only old-wang market and manual market note boxes.
* Do not change market rules, backend contracts, or generated outputs.
* Keep the new component file under 200 lines.

## TASK H77-01 — Extract Market Summary Boxes

Status: done

Goal:
Move two small market summary presentation boxes out of `Dashboard.tsx`.

Steps:

* Create `frontend/src/components/MarketSummaryBoxes.tsx`.
* Move `OldWangMarketBox`, `ManualMarketNoteBox`, and their display map.
* Keep existing props and rendered text.

Acceptance:

* Dashboard renders the moved components.
* No market strategy logic or data fetching is added.

Result:

* Added `frontend/src/components/MarketSummaryBoxes.tsx`.
* Dashboard imports `OldWangMarketBox` and `ManualMarketNoteBox`.

## TASK H77-02 — Verification

Status: done

Goal:
Verify the frontend still builds.

Steps:

* Run `npm run build` in `frontend`.
* Inspect `git diff --stat`.

Acceptance:

* Build passes.
* Only expected component/dashboard/task files changed.

Result:

* `npm run build` passed in `frontend`.
* Existing Vite chunk-size warning remains informational.

## Phase Checklist

* [x] H77-01 Extract market summary boxes
* [x] H77-02 Verification
