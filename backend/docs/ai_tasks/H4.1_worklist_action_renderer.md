# H4.1 Worklist Action Renderer Tasks

Phase: PM Worklist Action Renderer Consistency

Status: done

Read first:

* `backend/docs/ai_execution_plan.md`
* `backend/docs/ai_tasks/loop_state.md`
* `backend/docs/homepage_pm_roadmap.md`

Scope:

* Make PM Worklist, Daily Check, and Update Workflow actions feel consistent.
* Prefer rendering existing backend `action_payload` fields.
* Do not move strategy logic into frontend.
* Do not modify trading records, holdings, cash, or generated outputs.

## TASK H41-01 — Action Renderer Inventory

Status: done

Goal:
Find duplicated action button / copy command rendering across Dashboard workflow sections.

Steps:

* Inspect `PmWorklistBox`, `DailyCheckBox`, `UpdateWorkflowBox`, and Decision Console action handling.
* Identify one low-risk consolidation or display improvement.

Acceptance:

* Inventory notes list duplicated patterns and one safe target.
* No functional code changes in this task.

## TASK H41-02 — One Safe Action UX Improvement

Status: done

Goal:
Apply one small consistency improvement using existing backend payloads.

Steps:

* Keep action behavior unchanged.
* Prefer small helper or display tweak.
* Do not add new action types or frontend strategy rules.

Acceptance:

* One action flow is easier to understand or more consistent.

## TASK H41-03 — Verification

Status: done

Goal:
Verify the change.

Steps:

* Run `npm run build` from `frontend`.
* Check AI-facing markdown line counts.
* Check `git diff --stat`.

Acceptance:

* Build passes.
* AI-facing docs remain under 200 lines.

Verification result:

* `npm run build` passed.
* AI-facing docs checked under 200 lines.

## Inventory Notes

Status: filled

Findings:

* PM Worklist already renders `action_payload.expected_outputs` and `preview_items`.
* Daily Check renders `expected_outputs`, but not `preview_items`.
* Update Workflow renders expected outputs and copy command for `next_action`.
* Decision Console copies command / copy text for primary action.

Safe target:

* Add Daily Check `action_payload.preview_items` rendering using the existing chip style.
* Add the missing frontend type field.

## Phase Checklist

* [x] H41-01 Action renderer inventory
* [x] H41-02 One safe action UX improvement
* [x] H41-03 Verification
