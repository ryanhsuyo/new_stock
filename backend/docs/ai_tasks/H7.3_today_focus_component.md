# H7.3 Today Focus Component Boundary

Phase: Dashboard Gradual Split

Status: done

Read first:

* `backend/docs/ai_execution_plan.md`
* `backend/docs/ai_tasks/loop_state.md`
* `backend/docs/status_overview.md`
* `frontend/src/pages/Dashboard.tsx`
* `frontend/src/types/index.ts`

Scope:

* Extract only pure Today Focus presentation from `DecisionConsole`.
* Do not move PM priority or strategy logic into frontend.
* Do not change backend contracts or trading data.
* Keep the new component small and under 200 lines.

## TASK H73-01 — Extract Today Focus Card

Status: done

Goal:
Reduce `Dashboard.tsx` size by extracting the two Today Focus cards into a small component.

Steps:

* Create `frontend/src/components/TodayFocusCards.tsx`.
* Accept already-filtered focus item arrays from `DecisionConsole`.
* Preserve click behavior and text exactly where practical.
* Keep category decisions in `DecisionConsole` based on backend-provided `category`.

Acceptance:

* `DecisionConsole` renders the new component.
* Holding risk still appears before candidates/review.
* No new trading or strategy logic is added.

Result:

* Added `frontend/src/components/TodayFocusCards.tsx`.
* `DecisionConsole` now passes backend-derived focus arrays into that component.

## TASK H73-02 — Verification

Status: done

Goal:
Verify the frontend still builds.

Steps:

* Run `npm run build` in `frontend`.
* Inspect `git diff --stat`.

Acceptance:

* Build passes.
* Only expected frontend/component and AI task files changed.

Result:

* `npm run build` passed in `frontend`.
* Existing Vite chunk-size warning remains informational.

## Phase Checklist

* [x] H73-01 Extract Today Focus card
* [x] H73-02 Verification
