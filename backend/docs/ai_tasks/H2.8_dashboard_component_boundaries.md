# H2.8 Dashboard Component Boundaries Tasks

Phase: Dashboard Component Boundaries

Status: done

Read first:

* `backend/docs/ai_execution_plan.md`
* `backend/docs/ai_tasks/loop_state.md`
* `backend/docs/homepage_pm_roadmap.md`

Scope:

* Reduce `Dashboard.tsx` pressure with small, safe boundaries.
* Do not change API contracts or strategy logic.
* Do not introduce dependencies.
* Do not move large folders.

## TASK H28-01 — Boundary Inventory

Status: done

Goal:
Find one small component extraction that reduces Dashboard size without behavior changes.

Steps:

* Inspect Dashboard subcomponents.
* Pick only one low-risk extraction.
* Prefer pure presentational code with existing props.

Acceptance:

* Notes identify the chosen component and why it is safe.

## TASK H28-02 — Extract One Component

Status: done

Goal:
Move one small Dashboard subcomponent into `frontend/src/components/`.

Steps:

* Preserve props and behavior.
* Do not add strategy logic.
* Keep imports simple.

Acceptance:

* Dashboard imports the extracted component.
* No user-facing behavior changes intended.

## TASK H28-03 — Verification

Status: done

Goal:
Verify extraction.

Steps:

* Run `npm run build` from `frontend`.
* Check `git diff --stat`.

Acceptance:

* Build passes.
* AI-facing docs remain under 200 lines.

## Inventory Notes

Status: filled

Chosen extraction: `StatusRow` / `StatusDot` from `Dashboard.tsx`.

Why safe:

* Pure presentational file-status UI.
* Uses existing `FileInfo` props only.
* No API calls, no strategy logic, no state ownership.
* Existing Dashboard call sites can keep the same props.

Verification result:

* `npm run build` passed.
* AI-facing docs checked under 200 lines.

## Phase Checklist

* [x] H28-01 Boundary inventory
* [x] H28-02 Extract one component
* [x] H28-03 Verification
