# R2.1 Data Calendar, Coverage, And Lineage Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add local trading-calendar awareness, generated data coverage, and batch lineage so daily outputs can be audited before use.

**Architecture:** Keep routers thin and reuse existing daily workflow services. Add two focused services: one for trading-day decisions and one for coverage/lineage reporting. Existing scripts remain thin entry points.

**Tech Stack:** Python 3.11 standard library, existing JSON/CSV storage helpers, pytest

---

Status: done

Hard rules:

* Do not modify frontend.
* Do not manually edit generated `backend/out/*`.
* Do not modify trades, holdings, cash, or decision journals.
* Do not add dependencies, database, or new framework.
* Keep all tests network-free.

Read first:

* `backend/docs/current_rules.md`
* `backend/docs/signal_rules.md`
* `backend/docs/personal_product_readiness_roadmap.md`
* `backend/docs/ai_tasks/R2.0_data_calendar_lineage_design.md`

### Task 1: Activate R2.1 And Add Trading Calendar Service

**Files:**

* Create: `backend/app/services/trading_calendar_service.py`
* Create: `backend/tests/test_trading_calendar_service.py`
* Modify: `backend/app/utils.py`
* Modify: `backend/docs/ai_execution_plan.md`
* Modify: `backend/docs/ai_tasks/loop_state.md`

- [x] Set active phase to R2.1 and record the selected scope.
- [x] Add tests for weekend skip, configured holiday skip, configured makeup
  trading day count, malformed calendar fallback, and absent calendar fallback.
- [x] Run `/Library/Frameworks/Python.framework/Versions/3.11/bin/python3 -m pytest backend/tests/test_trading_calendar_service.py -q`; expect failure before the service exists.
- [x] Implement `load_trading_calendar(path=None)`, `is_trading_day(day, calendar=None)`,
  `previous_trading_day(day, calendar=None)`, and `count_missed_trading_days_since(data_date, today=None, calendar=None)`.
- [x] Update `app.utils.count_missed_trading_days()` to delegate to the service
  while preserving its signature.
- [x] Re-run the focused tests; expect pass.

### Task 2: Generate Coverage Report

**Files:**

* Create: `backend/app/services/data_coverage_service.py`
* Create: `backend/tests/test_data_coverage_service.py`
* Modify: `backend/app/services/workflow_outputs.py`

- [x] Add tests using temporary `leaders.json` and `ohlcv.csv` for statuses
  `ok`, `missing`, `insufficient`, and `lagging`.
- [x] Require report fields: `generated_at`, `batch_id`, `expected_trading_day`,
  `raw_ohlcv_as_of`, `tracked_count`, `ok_count`, `coverage_pct`, and `symbols`.
- [x] Run the focused coverage tests; expect import or assertion failures.
- [x] Implement pure coverage computation and `write_data_coverage_report(report, out_dir)`.
- [x] Add `backend/out/data_coverage_report.json` to the shared daily update
  expected outputs.
- [x] Re-run coverage tests; expect pass.

### Task 3: Attach Batch Lineage To Update And Signals

**Files:**

* Modify: `backend/app/services/update_service.py`
* Modify: `backend/app/services/signals_service.py`
* Modify: `backend/tests/test_update_status.py`
* Modify: `backend/tests/test_signals_api.py`
* Modify: `backend/tests/test_schedule.py`

- [x] Add tests that a full update writes one `batch_id` to update status,
  coverage report, and summary.
- [x] Add tests that manual `run_daily_signals()` still works without an
  injected batch and creates a local signal-only lineage object.
- [x] Run focused tests; expect missing lineage fields.
- [x] Generate batch IDs from UTC timestamp plus a short stable hash of started
  time, raw OHLCV mtime, and size.
- [x] Pass lineage metadata into `run_daily_signals(lineage=None)` and include
  it in `summary.json` without changing router logic.
- [x] Write coverage report after OHLCV is available and before final status is
  saved.
- [x] Re-run focused tests; expect pass.

### Task 4: Surface Coverage In Doctor And Workflow

**Files:**

* Modify: `backend/scripts/doctor.py`
* Modify: `backend/scripts/daily_check.py`
* Modify: `backend/app/services/update_workflow_service.py`
* Modify: `backend/tests/test_doctor.py`
* Modify: `backend/tests/test_daily_check.py`
* Modify: `backend/tests/test_update_workflow.py`

- [x] Add tests that low coverage appears as a warning/blocker with a command
  pointing to `python3 scripts/daily_update.py --months 1`.
- [x] Add tests that Update Workflow includes coverage path and batch ID in
  checks.
- [x] Run focused tests; expect failures because coverage is not surfaced.
- [x] Read `data_coverage_report.json` defensively; missing or invalid report
  becomes a warning with a regeneration command.
- [x] Preserve existing `action_payload` and `expected_outputs` conventions.
- [x] Re-run focused tests; expect pass.

### Task 5: Document, Verify, And Close R2.1

**Files:**

* Modify: `backend/docs/current_rules.md`
* Modify: `backend/docs/operations.md`
* Modify: `backend/docs/status_overview.md`
* Modify: `backend/docs/personal_product_readiness_roadmap.md`
* Modify: `backend/docs/ai_execution_plan.md`
* Modify: `backend/docs/ai_tasks/loop_state.md`
* Modify: `backend/docs/ai_tasks/R2.0_data_calendar_lineage_design.md`
* Modify: `backend/docs/ai_tasks/R2.1_data_calendar_lineage_implementation.md`

- [x] Document local calendar override format, coverage report, batch lineage,
  commands, and limitations.
- [x] Mark R2.0 implemented and R2.1 done, then clear active phase.
- [x] Run `/Library/Frameworks/Python.framework/Versions/3.11/bin/python3 -m pytest backend/tests -q`; full backend suite passed (`654 passed`).
- [x] Run `git diff --check`; no whitespace errors.
- [x] Confirm no frontend, formal trading records, or manually edited generated
  outputs were changed.

## Completion Checklist

* [x] Trading calendar service
* [x] Data coverage report
* [x] Batch lineage in update and signals
* [x] Doctor/Daily Check/Workflow surfacing
* [x] Documentation and full verification
