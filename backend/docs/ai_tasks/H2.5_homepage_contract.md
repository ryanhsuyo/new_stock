# H2.5 Homepage Contract Tasks

Phase: Homepage Contract & Documentation Reconciliation

Status: done

Read first:

* `backend/docs/ai_execution_plan.md`
* `AGENTS.md`
* `CLAUDE.md`
* `backend/docs/current_rules.md`
* `backend/docs/signal_rules.md`
* `backend/docs/homepage_pm_roadmap.md`

Scope:

* Stabilize backend Today Focus contract.
* Keep holding risk before candidate stocks.
* Add backend fixture tests.
* Reconcile stale docs.
* Keep frontend changes minimal and contract-driven.

## TASK H25-00 — Git Baseline

Status: todo

Goal:
Ensure the project has a git baseline before functional changes.

Steps:
1. Run `git rev-parse --show-toplevel`.
2. Run `git status --short`.
3. If this is not a git repo:
   * initialize git
   * create a reasonable `.gitignore`
   * create an initial baseline commit

Default tracking policy:
* Track source code, docs, tests, scripts, config, lockfiles, and small required sample files.
* Ignore cache/build/dependency files: `**pycache__/`, `.pytest_cache/`, `.mypy_cache/`, `.ruff_cache/`, `node_modules/`, `frontend/dist/`, `frontend/.vite/`, `.DS_Store`, `.env`, `.env.*`.
* Do not manually delete any file.
* `backend/out/**` is generated output. Ignore it unless an existing doc clearly says a specific file must be versioned.
* `backend/data/**` may contain user data. Track only project-required non-sensitive config/sample files, such as `leaders.json`, `stock_names.json`, or example files.
* Ignore personal trading records, positions, decision journals, market notes, and local fundamentals fill files if present.
* Suggested backend/data ignore examples: `backend/data/trades.json`, `backend/data/positions.json`, `backend/data/decision_journal.json`, `backend/data/market_notes.json`, `backend/data/fundamentals.csv`, `backend/data/fundamentals.json`, `backend/out/`.

Acceptance:
* `git status` works.
* Baseline commit exists, or there is an explicit report explaining why no baseline was created.
* No feature code is modified.
* `.gitignore` includes the chosen policy.
* Any ignored data/output files are listed in the report.

Commit rule:
* H25-00 is the only task in this phase where Claude Code may create a commit automatically.
* The baseline commit message must be: `chore: establish project baseline`
* After H25-00, do not commit unless explicitly requested.

Stop condition:
* Stop only if there is clear risk of committing secrets, credentials, tokens, private account data, or files that appear sensitive and cannot be safely categorized.

## TASK H25-01 — Contract Inventory

Status: todo
Goal:
Understand current system API contracts before changing them.
Steps:

* Read `backend/app/routers/system.py`.
* Read workflow / pm worklist / daily check services.
* Read related backend tests.
* Read `frontend/src/pages/Dashboard.tsx`.
* Add notes under "Contract Inventory Notes" in this file.

Acceptance:
* Notes list current fields for `/api/system/pm-worklist`, `/api/system/update-workflow`, and `/api/system/daily-check`.
* Notes say whether `today_focus` already exists.
* Notes say whether frontend builds focus items itself.

## TASK H25-02 — Today Focus Backend Contract

Status: todo
Goal:
Create or stabilize backend service-level `today_focus`.
Steps:

* Add or confirm `today_focus` in a system endpoint, preferably `/api/system/pm-worklist`.
* Generate it in `backend/app/services/**`, not router.
* Preserve existing `primary_action` and PM Worklist behavior.

Acceptance:
* Each item has `category`, `code`, `name`, `label`, `reason`, `next_action`, `severity`, `source`, `price_basis`, `as_of`.
* Categories: `portfolio_risk`, `entry_candidate`, `review_needed`.
* Each category has at most 3 items.
* `portfolio_risk` appears before `entry_candidate`.
* Blocked or stale trade outputs do not produce trade-like candidate actions.
* `price_basis` clearly says latest close / non-realtime.

## TASK H25-03 — Fixture Tests

Status: todo
Goal:
Add backend tests for homepage PM contract states.
Steps:

* Add fixed fixture tests for ready, blocked, stale, and empty states.
* Prefer service-level tests if API-level fixtures are too heavy.
* Do not depend on live network or TWSE data.

Acceptance:
* Ready: `today_focus` exists, max 3 per category, has `price_basis` and `as_of`.
* Blocked: `primary_action` points to blocker; candidates are not buy-now actions.
* Stale: refresh command/status is available; price basis remains clear.
* Empty: no holdings/candidates does not crash and returns reason / next_action.
* `python3 -m pytest backend/tests -q` passes.

## TASK H25-04 — Minimal Frontend Consumption

Status: todo
Goal:
Make Dashboard consume backend `today_focus` if it currently builds focus priority itself.
Steps:

* Add frontend type for `today_focus`.
* Render backend-provided categories.
* Keep changes small; do not split `Dashboard.tsx` unless clearly safe.

Acceptance:
* Dashboard renders backend `today_focus`.
* Holding risk appears before candidates.
* Price basis is visible.
* No frontend strategy rules are added.

## TASK H25-05 — Documentation Reconciliation

Status: todo
Goal:
Update stale docs to match actual implementation.
Steps:

* Check `README.md`, `backend/README.md`, `backend/docs/daily_runbook.md`, `backend/docs/status_overview.md`, `backend/docs/phases.md`, `backend/docs/homepage_pm_roadmap.md`.
* Correct page/tab count.
* Correct signals run sync/async status.
* Correct TPEX and head-and-shoulders-bottom status.
* Ensure daily operation uses `python3 scripts/daily_update.py --months 1`.
* Add Dashboard reading order: Status Strip, Primary Action, Market Posture, Today Focus, PM Worklist, Daily Check / Workflow / file health.

Acceptance:
* Docs reflect actual implementation.
* Unfinished items are not described as complete.
* Daily runbook explains how to read the new Dashboard.

## TASK H25-06 — Verification

Status: todo
Goal:
Verify the phase.
Steps:

* Run `python3 -m pytest backend/tests -q` from project root.
* Run `npm run build` from `frontend`.
* Confirm build artifacts are ignored.

Acceptance:
* Backend tests pass.
* Frontend build passes.
* Exact failures and safest next step are reported if verification fails.

## Contract Inventory Notes

Status: empty

Fill during H25-01.

## Phase Checklist

* [x] H25-00 Git baseline
* [ ] H25-01 Contract inventory
* [ ] H25-02 Today Focus backend contract
* [ ] H25-03 Fixture tests
* [ ] H25-04 Frontend minimal consumption
* [ ] H25-05 Documentation reconciliation
* [ ] H25-06 Verification
