# R4.1 Signal Snapshot And Next-Day Review Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Save daily signal snapshots and produce a next-day review comparing the prior plan to the current signal run.

**Architecture:** Add a focused `signal_snapshot_service` under services, integrate it into `run_daily_signals`, and document the generated outputs. The service owns file I/O and comparison logic; routers remain unchanged for the first version.

**Tech Stack:** Python, pytest, JSON, pathlib, existing `atomic_write_text`.

---

## Task 1: Snapshot Service

**Files:**
- Create: `backend/app/services/signal_snapshot_service.py`
- Create: `backend/tests/test_signal_snapshot_service.py`

- [x] Build compact snapshot rows from summary signals.
- [x] Write timestamped snapshot files under `signal_snapshots/`.
- [x] Find the latest previous snapshot before current `as_of`.
- [x] Write review outcomes: `risk_triggered`, `risk_eased`, `action_changed`, `unchanged`, `missing_current`.
- [x] Run `python3 -m pytest backend/tests/test_signal_snapshot_service.py -q`.

## Task 2: Signals Integration

**Files:**
- Modify: `backend/app/services/signals_service.py`
- Modify: `backend/tests/test_signals_api.py`
- Modify: `backend/tests/test_run_signals_cli.py`

- [x] Call the snapshot service after primary outputs are written.
- [x] Add `signal_snapshot_review.json` and the snapshot path to `files_written`.
- [x] Test that `run_daily_signals()` creates snapshot and review outputs.
- [x] Update CLI summary test to include snapshot review in primary outputs if printed.

## Task 3: Docs And State

**Files:**
- Modify: `backend/docs/current_rules.md`
- Modify: `backend/docs/api.md`
- Modify: `backend/docs/personal_product_readiness_roadmap.md`
- Modify: `backend/docs/ai_tasks/loop_state.md`
- Modify: `backend/docs/ai_execution_plan.md`

- [x] Document snapshot and review semantics.
- [x] Mark R4.1 complete only after verification.
- [x] Keep review wording conservative: audit aid, not performance proof.

## Task 4: Verification

- [x] `python3 -m pytest backend/tests/test_signal_snapshot_service.py backend/tests/test_signals_api.py::TestSignalsOutput -q`
- [x] `python3 -m pytest backend/tests -q`
- [x] `cd frontend && npm run build`

## Stop Conditions

- Stop if comparison requires intraday data or real brokerage data.
- Stop if implementation would modify `backend/data/trades.json` or decision journal.
