# H7.5 Primary Action Card Boundary

Phase: Dashboard Gradual Split

Status: done

Read first:

* `backend/docs/ai_execution_plan.md`
* `backend/docs/ai_tasks/loop_state.md`
* `frontend/src/pages/Dashboard.tsx`
* `frontend/src/types/index.ts`

Scope:

* Extract only the Primary Action card presentation.
* Keep primary-action selection in `DecisionConsole`.
* Do not change backend contracts, trading data, or generated outputs.
* Keep the new component under 200 lines.

## TASK H75-01 — Extract Primary Action Card

Status: done

Goal:
Reduce `Dashboard.tsx` by moving the Primary Action card into a small component.

Steps:

* Create `frontend/src/components/PrimaryActionCard.tsx`.
* Pass `primaryAction`, `statusClass`, `busy`, `copied`, and an action callback.
* Preserve text, expected outputs, and button behavior.

Acceptance:

* `DecisionConsole` renders the new component.
* Primary action priority still comes from backend `worklist.primary_action`.
* No PM priority logic is added to the new component.

Result:

* Added `frontend/src/components/PrimaryActionCard.tsx`.
* `DecisionConsole` still selects `worklist.primary_action` and passes it to the component.

## TASK H75-02 — Verification

Status: done

Goal:
Verify the frontend still builds.

Steps:

* Run `npm run build` in `frontend`.
* Inspect `git diff --stat`.

Acceptance:

* Build passes.
* Only expected component/dashboard/task files changed.

Result:

* `npm run build` passed in `frontend`.
* Existing Vite chunk-size warning remains informational.

## Phase Checklist

* [x] H75-01 Extract primary action card
* [x] H75-02 Verification
