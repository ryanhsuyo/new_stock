# H8.2 Secondary Dashboard Disclosure

Phase: Secondary Dashboard Disclosure

Status: done

Goal:

Reduce below-the-fold text density while preserving every existing Dashboard action and detail.

Design:

Use native `details` groups. The Decision Console remains always visible. Secondary content is grouped by user intent instead of data source.

Hard rules:

* Do not modify backend strategy or trading data.
* Do not remove actions or explanations.
* Existing focus links must open the containing details group automatically.
* Do not add dependencies or broadly refactor Dashboard.
* Keep this file under 200 lines.

## TASK H82-00 — Group Secondary Sections

Status: done

Goal:

Create four compact below-the-fold groups.

Steps:

* Group decision journal as `決策復盤`.
* Group market note, Old Wang summary, and Daily Brief as `盤後研究`.
* Group fundamentals, update actions, file health, and change report as `基本面與系統維護`.
* Group recommendation cards as `推薦卡片`.
* Keep all groups collapsed by default.

Acceptance:

* Dashboard no longer renders long secondary sections by default.
* No existing action or data block is removed.
* Decision Console remains first and always visible.

## TASK H82-01 — Navigation and Mobile Verification

Status: done

Goal:

Verify disclosure behavior and responsive layout.

Steps:

* Confirm a focus action opens its containing details group.
* Confirm each summary toggles independently.
* Check desktop and mobile document overflow.
* Run frontend build.

Acceptance:

* Focus navigation does not target hidden content.
* No horizontal document overflow at 1280x720 or 390x844.
* `npm run build` passes.

## Phase Checklist

* [x] H82-00 Group secondary sections
* [x] H82-01 Navigation and mobile verification

## Verification Notes

* Dashboard now has exactly four collapsed native `details` groups.
* PM Worklist, update workflow, Daily Check, repair queue, workflow status,
  fundamentals, file health, and change report share the system maintenance group.
* Focus targets remain inside their owning group; `focusSection` opens the nearest
  `details` before scrolling.
* `npm run build` passed.
* Formal browser viewport checks remain soft-blocked because the in-app browser
  timed out loading localhost. Structural checks passed and no width CSS changed.
