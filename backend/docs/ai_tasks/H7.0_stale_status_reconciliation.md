# H7.0 Stale Status Reconciliation Tasks

Phase: Stale Status Reconciliation

Status: done

Read first:

* `backend/docs/ai_execution_plan.md`
* `backend/docs/ai_tasks/loop_state.md`
* `backend/docs/status_overview.md`

Scope:

* Verify stale status uses trading-day logic.
* Reconcile outdated docs if implementation already exists.
* Keep changes small and test-backed.
* Do not modify generated `backend/out/*`.

## TASK H70-01 — Stale Logic Inventory

Status: done

Goal:
Confirm whether stale status still uses calendar days or already uses trading-day logic.

Steps:

* Inspect `backend/app/services/update_service.py`.
* Inspect `backend/app/utils.py`.
* Inspect existing stale tests.

Acceptance:

* Inventory notes identify actual implementation state.

## TASK H70-02 — Test Weekend Freshness

Status: done

Goal:
Add or confirm a focused test that Friday data is not stale on the following Monday before close-day inclusion.

Steps:

* Write a failing test first if coverage is missing.
* Keep test deterministic by monkeypatching the service date provider if available.
* Avoid network and real market data.

Acceptance:

* Test proves weekend days alone do not mark data stale.

Result:

* Added deterministic regression test for Friday data checked on Monday.
* Focused test passed; no production stale logic change required.

## TASK H70-03 — Docs Reconciliation

Status: done

Goal:
Update docs if stale trading-day logic is already complete.

Steps:

* Remove stale item from high-priority todo if complete.
* Keep remaining risks factual.

Acceptance:

* Docs no longer list completed stale behavior as a blocker.

Result:

* Updated stale wording in status overview, API docs, and operations docs.
* Removed stale trading-day fix from high-priority unfinished items.

## TASK H70-04 — Verification

Status: done

Goal:
Verify backend and docs.

Steps:

* Run focused stale tests.
* Run `python3 -m pytest backend/tests -q` if focused tests pass.
* Check AI-facing markdown line counts.

Acceptance:

* Tests pass.
* AI-facing docs remain under 200 lines.

Verification result:

* `python3 -m pytest backend/tests/test_update_status.py::TestStaleCalculation -q`: 9 passed.
* `python3 -m pytest backend/tests -q`: 558 passed.
* AI-facing docs checked under 200 lines.

## Inventory Notes

Status: filled

Findings:

* `update_service._compute_stale()` already calls `count_missed_trading_days(data_date)`.
* `count_missed_trading_days()` counts weekdays after `data_date` and before today.
* `stale_days` remains calendar days for display, while `is_stale` uses trading days.
* Docs still list stale trading-day logic as a high-priority unfinished item.

Need:

* Add a focused deterministic test for weekend-only gap behavior before updating docs.

## Phase Checklist

* [x] H70-01 Stale logic inventory
* [x] H70-02 Test weekend freshness
* [x] H70-03 Docs reconciliation
* [x] H70-04 Verification
