# Tasks

## 1. 前推驗收（唯讀，先不動任何清單）

- [x] 1.1 新增前推驗收服務：讀 `out/signal_snapshots/`，以 `generated_at` 次一交易日開盤為進場基準，用訊號自帶 `daily_invalidation` 出場。
- [x] 1.2 缺 `daily_invalidation` 的訊號標 `no_stop_defined` 並排除於勝率統計，不補值。
- [x] 1.3 已平倉與未平倉分開統計；輸出樣本區間、筆數、平均、中位、勝率。
- [x] 1.3b 回看窗口固定為最新資料日往前 1 個月；輸出實際涵蓋的快照日數，不假設連續。窗口內無訊號時輸出樣本數 0，不得自動延長窗口。
- [x] 1.4 加同期市場中位與基準指數對照欄位。
- [x] 1.5 輸出可稽核 artifact 至 `backend/out/`，逐筆含 code、訊號日、進場日、進場價、出場日、出場價、出場原因。
- [x] 1.6 固定測試：延後產出的快照不得以 `as_of` 次日進場；驗證 `as_of=2026-06-26` / `generated_at=2026-06-30` 的進場日落在 06-30 之後。
- [x] 1.7 用現有快照重跑驗證基準：全部 54 筆為平均 -6.94% / 勝率 11.1%，近 1 個月（07-01 起）37 筆、已出場 32 筆、平均 -6.35% / 勝率 10.8%。兩組數字都要能重現。

## 2. 報告證據狀態區塊

- [x] 2.1 台股報告在「今日結論」之前新增證據狀態區塊，印樣本數、平均、勝率、同期市場中位、最近一次護欄回放結果。
- [x] 2.2 美股報告沿用同一契約；無快照時印樣本數 0 與「尚無可驗收樣本」，不得借用台股數字。
- [x] 2.3 樣本不足時印「證據不足」與實際筆數，不得留白。
- [x] 2.4 固定測試：證據狀態出現在今日結論之前；平均為負時該數字必須出現在候選清單之前。
- [x] 2.5 不新增綜合評分、健康度或策略排名欄位（反向測試：確認這些字串不出現）。

## 3. 護欄接上 production 進場清單

- [x] 3.1 台股報告改為 per-profile 輸出三份（combined / old_wang / steady_momentum），各自套用該 profile 上限；決定檔名規則。
- [x] 3.1b 三份報告各自印出「上限僅在本報告內有效，同時依多份進場會放大實際曝險」警告。
- [x] 3.2 定義盤前風險降級推估規則：缺 SPY / 缺 QQQ / 缺 TSM / 全缺各對應哪一級，寫成表並附測試；推估結果標 `degraded` 並列缺漏項。
- [x] 3.2b 驗證推估等級不得寬鬆於可得資料所支持的等級（反向測試：缺資料不得產生比較安全的結論）。
- [x] 3.3 `_tw_current_rows()` 的進場篩選套用護欄，重用回放既有 helper，不另建平行實作。
- [x] 3.4 被擋候選移入「今日不新增（風控擋下）」區塊，帶人可讀原因，不得只印 reason_code。
- [x] 3.5 報告印出分桶合計並驗證加總回候選總數。
- [x] 3.6 護欄接線以單一開關控制，關閉即回到現行行為。
- [x] 3.7 固定測試：defensive 時進場清單為空且候選全數落入擋下區塊；每日新倉上限超出時只保留上限內檔數；同一檔可在某 profile 進場、在另一 profile 被擋。
- [x] 3.8 固定測試：護欄不得修改賣出、減碼與出場警示路徑。
- [x] 3.9 `scheduled_strategy_report.py` 產生三份台股 artifact；成功標記改為 per-artifact，任一份失敗不得標記整體成功，並補測試。

## 4. 快照補存風控狀態

- [x] 4.1 `_compact_signal` 補存 `old_wang_market_filter` 與 `old_wang_market_regime`。
- [x] 4.2 快照層級補存當日盤前風險等級。
- [x] 4.3 前推驗收優先讀快照風控欄位；缺欄位時回退為由指數重算，並在輸出標示為重建值。
- [x] 4.4 固定測試：新快照含風控欄位；舊快照缺欄位時走重建路徑且標示正確。

## 5. 驗收與交接

- [x] 5.1 同一資料日下比對護欄接上前後的候選差異，逐檔確認擋單原因正確；三個 profile 各比對一次。
- [x] 5.2 重跑台股三份與美股一份報告，人工檢查證據狀態數字與 1.7 的驗收結果一致。
- [x] 5.3 完整 backend 回歸測試綠燈。
- [x] 5.4 更新既有 `strategy-portfolio-guardrails` 的 evaluation-only 條文，指向本 change 取代。
- [x] 5.5 交接紀錄：明列本 change 沒有做的事（未調參數、未新增策略、未宣稱績效改善）。
