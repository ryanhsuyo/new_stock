# Tasks

## 1. 美股快照

- [x] 1.1 新增美股快照服務：從 `_us_watch_rows()` 的結果建立快照，含 code、名稱、桶別、動作、策略、收盤、觸發價、失效價、觀察目標。
- [x] 1.2 快照層級保存趨勢濾網 bias / active、W 底濾網 active、資料日與 `generated_at`。
- [x] 1.3 寫出 `out/us_signal_snapshots/us_signal_snapshot_<as_of>.json`；同一資料日重複執行覆寫而非新增。
- [x] 1.4 產生美股報告時一併寫出快照；寫檔失敗時該次執行視為失敗。
- [x] 1.5 固定測試：全部桶都被保存（含已失效）；同一 as_of 執行兩次只留一份。

## 2. 前推驗收支援美股

- [x] 2.1 前推驗收加入市場參數，美股讀 `us_signal_snapshots`、用美股欄位名，不硬套台股欄位。
- [x] 2.2 只有「可紙上追蹤」桶計入績效；其餘桶保存但不計分。
- [x] 2.3 出場只用型態失效價，不用量幅目標；收盤跌破後次一交易日開盤出場。
- [x] 2.4 兩市場結果分開統計，不得合併平均或勝率。
- [x] 2.5 固定測試：觀望桶不計入樣本；觸及目標價不自動出場；美股 0 樣本時不顯示台股數字。

## 3. 報告串接

- [x] 3.1 美股報告證據狀態改用實際快照計算，取代目前寫死的 0 樣本。
- [x] 3.2 樣本不足時仍明示實際筆數與涵蓋快照日數。
- [x] 3.3 `scheduled_strategy_report.py` 的美股 artifact 清單加入當日快照。
- [x] 3.4 固定測試：美股證據狀態的數字來自美股快照，與台股互不影響。

## 4. 驗收與交接

- [x] 4.1 產生一次美股報告，確認快照寫出且內容與報告分桶一致。
- [x] 4.2 完整 backend 回歸測試綠燈。
- [x] 4.3 交接紀錄：明列未做的事（未補建歷史、未改策略、樣本累積慢是預期）。
